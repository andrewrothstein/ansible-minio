---
minio_app_name: '{{ minio_app.name }}.{{ minio_app.ver }}'
minio_app_url: '{{ minio_mirror }}/{{ minio_app.subpath }}/{{ minio_app.name }}/release/{{ minio_platform }}/archive/{{ minio_app_name }}'
minio_app_exe: '{{ minio_install_dir }}/{{ minio_app_name }}'
minio_app_link: '{{ minio_install_dir }}/{{ minio_app.name }}'
minio_app_checksum_url: '{{ minio_app_url }}.sha256sum'
